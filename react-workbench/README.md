# SmartPlace React Workbench

## Start

Backend:

```bash
uvicorn backend.main:app --host 0.0.0.0 --port 8000
```

Frontend:

```bash
npm install
npm run dev
```

The frontend expects the backend at `http://127.0.0.1:8000`.
