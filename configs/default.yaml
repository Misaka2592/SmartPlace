project:
  name: SmartPlace
  version: v0.2

scorer:
  name: DummyScorerV2
  weight_path: weights/dummy_scorer_rule_based.pth
  device: cpu
  input_size: 224
  batch_size: 8

threshold:
  recommend: 0.75
  acceptable: 0.45

output:
  save_images: true
  save_csv: true
  save_log: true